# Everything needed to run the whole project from a checkout:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
-r backend/requirements.txt
-r model/requirements.txt
